import { TreeListDraggableDirective } from './tree-list-draggable.directive';

describe('TreeListDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeListDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
