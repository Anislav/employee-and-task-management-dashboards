import { GridDroppableDirective } from './grid-droppable.directive';

describe('GridDroppableDirective', () => {
  it('should create an instance', () => {
    const directive = new GridDroppableDirective();
    expect(directive).toBeTruthy();
  });
});
